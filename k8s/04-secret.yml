apiVersion: v1
kind: Secret
metadata:
  name: dev-instance-dns-sync-secret
  namespace: dev-instance-dns-sync
type: Opaque
data:
  # CLOUDFLARE_API_TOKEN: <base64-encoded-token>
  # You need to base64 encode your Cloudflare API token and replace the value above
  # Example: echo -n "your-api-token" | base64
  CLOUDFLARE_API_TOKEN: ""