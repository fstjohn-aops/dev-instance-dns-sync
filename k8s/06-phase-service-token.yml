apiVersion: v1
kind: Secret
metadata:
  name: phase-service-token
  namespace: dev-instance-dns-sync
type: Opaque
data:
  # token: <base64-encoded-phase-service-token>
  # You need to base64 encode your Phase service token and replace the value above
  # Example: echo -n "your-phase-service-token" | base64
  token: "" 